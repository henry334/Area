import { Controller, Post, Body, Get, UseGuards, Req, Injectable } from '@nestjs/common';

@Controller('nothing for the moment but it will be the path to trigger action controller when it will be created in the future if we have time to do it ! jeremy and me are too busy with the front and the back to do it for the moment ! sorry ! :/ for the moment, we are just doing the trigger logic and the trigger action module ! :/ keep going !')
export class TriggerActionController {
}